export class Item {
  name: string
  luminosity: number
  icon: string

  constructor(name: string, luminosity: number, icon: string) {
    this.name = name
    this.luminosity = luminosity
    this.icon = icon
  }
}
